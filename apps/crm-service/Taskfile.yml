version: '3'

tasks:
  build:
    cmds:
      - go build -o bin/crm-service ./cmd/crm-service

  run:
    cmds:
      - go run ./cmd/crm-service

  test:
    cmds:
      - go test ./...

  lint:
    cmds:
      - go vet ./...

  docker:
    cmds:
      - docker build -t crm-service:latest .
